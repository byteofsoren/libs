// Prototype def

#ifndef TUNA_STR
#define TUNA_STR

    extern int StringNumberCounter(const char *text);
    extern int StrToInt(const char *text, int *error);
    extern float StrToFloat(const char *text, int *error);
    // Test

#endif
